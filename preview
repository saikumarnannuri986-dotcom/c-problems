#include <stdio.h>

int main() {
    int arr[] = {10, 20, 30, 40, 50};
    int size = sizeof(arr) / sizeof(arr[0]);
    int preview_count = 3;

    for (int i = 0; i < preview_count && i < size; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");

    return 0;
}
